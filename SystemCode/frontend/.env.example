# Optional: force API origin. If unset in dev, requests use same origin + Vite proxy (see vite.config.js).
# Production build defaults to http://localhost:8000 unless you set this at build time.
# VITE_API_BASE_URL=http://localhost:8000
