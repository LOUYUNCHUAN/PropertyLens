# PropertyLens 架构总览

本项目采用四层架构：
- 数据层（Data Layer）：原始数据 -> 清洗 -> 地理特征 -> 训练数据集
- ML层（ML Layer）：特征训练、分层模型、反馈回流数据层
- 服务层（Service Layer）：搜索服务 + 价格预测服务
- 可解释层（Explainability Layer）：解释各因素对价格贡献占比

核心目标：
- 用显式条件（如价格、房间数、楼层、租约）+ 隐式条件（如MRT/食阁/学校档次）进行房源筛选
- 输入具体房源信息，输出预测价格
- 输出每个因素对价格的贡献占比（可解释性）
